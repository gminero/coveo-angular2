import { CoveohookDirective } from './coveohook.directive';

describe('CoveohookDirective', () => {
  it('should create an instance', () => {
    const directive = new CoveohookDirective();
    expect(directive).toBeTruthy();
  });
});
